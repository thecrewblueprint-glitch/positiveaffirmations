# CLAUDE.md — Entry guidance

Read `AGENTS.md` first.

This repository is a **dormant** standalone affirmations application. Do not run, deploy, reconnect OAuth, schedule jobs, or modernize it merely because the code exists.

Accepted branch is `main`. Substantive work requires explicit owner reactivation and then follows the matrix PR-first change-control model. Preserve authentication, privacy, secret-handling, and external-service boundaries in `AGENTS.md`.
